export enum SmartContractEvents {
  payather = "payather",
  claimtoken = "claimtoken",
  afterlist = "after_list",
  after_buy = "after_buy",
}

// event after_list(address owner, uint256 price, uint256 tokenid);
// function list_nft(uint tokenID, uint newRatio)
